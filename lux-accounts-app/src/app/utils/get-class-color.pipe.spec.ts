import { GetClassColorPipe } from './get-class-color.pipe';

describe('GetClassColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GetClassColorPipe();
    expect(pipe).toBeTruthy();
  });
});
